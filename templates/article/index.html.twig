{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}


{% for articles in article %}


<table class="table">
  <thead>
    <tr>
      <th scope="col"></th>
      <th scope="col">Titre</th>
      <th scope="col">Content</th>
      <th scope="col">Date</th>
      <th scope="col">auteur</th>
      <th scope="col">categorie</th>
      <th scope="col">nombre de vue</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1</th>
      <td>{{ article.title }}</td>
      <td>{{ article.content}}</td>
      <td>{{ article.date}}</td>
      <td>{{ article.author}}</td>
      <td>{{ article.categorie}}</td>
      <td>{{article.view}}</td>
    </tr>
    <tr>
      <th scope="row">2</th>
      <td>Jacob</td>
      <td>Thornton</td>
      <td>@fat</td>
    </tr>
    <tr>
      <th scope="row">3</th>
      <td colspan="2">Larry the Bird</td>
      <td>@twitter</td>
    </tr>
  </tbody>
</table>
    {% endfor %}


{# <h2> Les derniers sujets : 
{% for subject in subjects %}
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">Sujet: {{ subject.title }}</h5>
    <h6 class="card-subtitle mb-2">Posté le {{ subject.published|date('d/m/y')}} à {{ subject.published|date('H:i')}}</h6>
    <p class="card-text">{{subject.content}}</p>
    <a href="#" class="card-link">Voir le sujet</a>
  </div>
</div>
{% endfor %} #}

{% endblock %}
